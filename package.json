{
  "name": "proximity",
  "version": "0.1.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "frontend:dev": "npm run dev --workspace=frontend",
    "frontend:build": "npm run build --workspace=frontend",
    "backend:dev": "npm run dev --workspace=backend",
    "backend:build": "npm run build --workspace=backend",
    "server:build": "npm run build --workspace=server",
    "adapter:build": "npm run build --workspace=adapter",
    "adapter:dev": "npm run dev --workspace=adapter",
    "adapter:test": "npm run test --workspace=adapter"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^5.45.1",
    "@typescript-eslint/parser": "^5.45.1",
    "eslint": "^8.29.0",
    "mocha": "^10.1.0",
    "nyc": "^15.1.0",
    "typescript": "^4.9.3",
    "@types/mocha": "^10.0.1",
    "@types/node": "^18.11.11",
    "debug": "^4.3.4",
    "ts-node": "^10.9.1"
  }
}
